{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DetailCmd</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ entity.quantite }}</td>
            </tr>
            <tr>
                <th>Gadgetquantite</th>
                <td>{{ entity.gadgetQuantite }}</td>
            </tr>
            <tr>
                <th>Pghtactuel</th>
                <td>{{ entity.pGHTActuel }}</td>
            </tr>
            <tr>
                <th>Commentaire</th>
                <td>{{ entity.commentaire }}</td>
            </tr>
            <tr>
                <th>Matmemopghtactuel</th>
                <td>{{ entity.matMemoPGHTActuel }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('detailcmd') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('detailcmd_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
