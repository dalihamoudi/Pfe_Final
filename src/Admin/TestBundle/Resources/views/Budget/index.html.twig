{% extends 'base.html.twig' %}


{% block body %}
    <h1>Depence budgets list</h1>
    {{ render(controller('AdminTestBundle:Budget:new')) }}



    <!-- HTML DATATABLE -->
    <table class="table table-striped table-bordered table-hover" id="sample_3">
        <thead>
        <tr>
            <th>
                Groupe de produit</th>
            <th>
                Date
            </th>
            <th class="hidden-xs">
                Number
            </th>
            <th class="hidden-xs">
                Prix Ht        </th>
            <th class="hidden-xs">
                Quantité
            </th>
            <th class="hidden-xs">
                Valeur
            </th>
            <th class="hidden-xs">
                Etat
            </th>

        </tr>
        </thead>
        <tbody>

        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('budget_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.PFRubgroupanneebudget }}</td>
                <td>{{ entity.Annee }}</td>
                <td>{{ entity.Rubrique }}</td>
                <td>{{ entity.GP }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('budget_show', { 'id': entity.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('budget_edit', { 'id': entity.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
{% endblock %}