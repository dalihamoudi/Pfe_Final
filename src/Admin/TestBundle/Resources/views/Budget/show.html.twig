{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Budget</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Pf_rub_group_annee_budget</th>
                <td>{{ entity.PFRubgroupanneebudget }}</td>
            </tr>
            <tr>
                <th>Annee</th>
                <td>{{ entity.Annee }}</td>
            </tr>
            <tr>
                <th>Rubrique</th>
                <td>{{ entity.Rubrique }}</td>
            </tr>
            <tr>
                <th>G_p</th>
                <td>{{ entity.GP }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('budget') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('budget_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
